uniform vec2 uResolution;
uniform float uTime;
varying vec2 vUv;

void main() {
    // Convert your ShaderToy code here
    // Replace iResolution with uResolution, iTime with uTime, iChannel0 with uTexture0, etc.
    // Use vUv for the UV coordinates
    vec2 uv = vUv;

    // Your ShaderToy mainImage code goes here
    // Adapt it to set gl_FragColor
}
